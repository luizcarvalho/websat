<h1><%=h labels('new') + " " + labels('registration') %></h1>

<%= error_messages_for :menu %>

<% form_for(:menu, :url => collection_url) do |f| %>
  <%= render :partial => "form", :locals => { :f => f, :button_name=> labels('create') } %>
<% end %>

<%= link_to labels('back'), collection_url %>