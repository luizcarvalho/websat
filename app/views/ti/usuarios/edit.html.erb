<h1><%=h labels('changing') + " " + labels('registration') %></h1>

<%= error_messages_for :usuario %>

<% form_for(:usuario, :url => object_url, :html => { :method => :put }) do |f| %>
  <%= render :partial => "form", :locals => { :f => f } %>
  <p>
    <%=submit_tag labels('update')%>
  </p>
<% end %>

<%= link_to labels('show'), object_url %>
|
<%= link_to labels('back'), collection_url %>